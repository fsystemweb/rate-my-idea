{
  "$schema": "https://turborepo.org/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"],
      "env": ["MONGODB_URI", "DEV"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}